---
import { ventures } from '@/data/ventures';

export function getStaticPaths() {
  return ventures.map((venture) => ({ params: { slug: venture.slug } }));
}

const { slug } = Astro.params;
return Astro.redirect(`/portfolio/${slug ?? ''}`, 301);
---
